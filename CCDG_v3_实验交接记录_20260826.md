# CCDG v3 纯间隙提取与通信时长建模 —— 实验交接记录（2026-08-26）

> 用途：对话窗口切换时的完整上下文交接。本记录覆盖 v3 全链路 11 项任务（全部 COMPLETE）的环境、结果与问题。配套 skill：`/lmp-booksim`（含 v2 及更早的完整历史结论）。

## 1. 当前状态

**v3 全链路 11 项任务全部完成**（2026-08-26）：

| # | 任务 | 状态 |
|---|---|---|
| 1 | dumpi2ccdg：时间函数拆分 + last_cpu_stop + ~15 回调纯间隙改造 | ✅ |
| 2 | dumpi2ccdg：compute_ops 反推 + JSON 输出 + LEGACY 兼容 | ✅ |
| 3 | 编译 dumpi2ccdg + 回归（LEGACY == 旧 compact_v2） | ✅ |
| 4 | 生成 4 个 run 目录 compact_v3（TRIM_SETUP 不折叠） | ✅ |
| 5 | 计算量守恒检查（Σcompute_ops vs log 相位） | ✅ |
| 6 | BookSim：compute_ops 解析 + ccdg_compute_rate + 停留公式 | ✅ |
| 7 | BookSim：blocked_cycles/blocked_ratio 统计 | ✅ |
| 8 | 编译 BookSim + 回归（旧 CCDG == CSV 基线） | ✅ |
| 9 | SimGrid 结构验证 4/4（固定频率仅结构判据） | ✅ |
| 10 | BookSim free 基线 4 组 + 时间/blocked_ratio 对照 | ✅ |
| 11 | WSE 复测（64r short）+ 结果汇总 CSV | ✅ |

计划文件（勿编辑）：`/root/.config/QoderCN/someValue.machineId/SharedClientCache/cache/plans/CCDG纯间隙提取与通信时长建模_4e6e2a70.md`

## 2. 实验背景与动机（一句话）

旧 `dumpi2ccdg` 用 `cpu_time_to_sec = (start+stop)/2` 提取 COMPUTE，MPI 内部 busy-wait 自旋的 CPU 消耗被均摊进相邻 COMPUTE → ΣCOMPUTE ≈ Loop time（100%），真实通信占比被抹掉（log 口径 64r short 通信 55.3% vs BookSim 只显式 10.4%）。**v3 = 纯间隙提取 + 计算量反推（roofline）**，仿真边界：软件开销不建模，只评估硬件（计算+网络）。

## 3. 实验环境（v3 相关，已验证）

| 项 | 值 |
|---|---|
| dumpi2ccdg v3 | `/work1/jiangtao/lammps_trace/dumpi2ccdg/dumpi2ccdg`（源码 `dumpi2ccdg.cpp`；运行需 `LD_LIBRARY_PATH=/work1/jiangtao/lammps_trace/install/lib`） |
| dumpi2ccdg v3 环境变量 | `CCDG_CPU_FREQ_GHZ`（默认 2.5，compute_ops 反推频率）；`CCDG_LEGACY_CPU_MID=1`（切回旧 (start+stop)/2 行为）；`CCDG_TRIM_SETUP=1`（窗口裁剪）；**v3 不启用 CCDG_COMPACT 折叠** |
| BookSim 可执行 | `/work1/jiangtao/lammps_trace/booksim2/booksim`（src 下 `make` 后 `cp src/booksim .`） |
| BookSim 核心改造 | `booksim2/src/ccdg_trafficmanager.{hpp,cpp}`：`ccdg_compute_capability`（ops/s 输入参数，2026-08-26 改，优先于 ccdg_compute_rate）、`blocked_cycles`/`blocked_ratio` 统计、`wse_inject_blocked_cycles` |
| BookSim v3 关键配置 | **计算能力参数 `ccdg_compute_capability=2.5e9 ops/s`**（= 2.5GHz×1op/cycle，等价旧 ccdg_compute_rate=1.25 ops/cycle @ noc 2.0GHz）；换算：`compute_time_sec = compute_ops/capability`，`cycles = time × noc_freq`；实际运行 cfg 用 `cpu_frequency_ghz=2.5( trace 机,仅 legacy 回退)、noc_frequency_ghz=2.0`（墙钟 = cycles × 0.5ns） |
| 注入脚本 | `booksim2/run_ccdg_mesh.sh <ccdg文件> [超时秒] [步数]` |
| 结果 CSV | `booksim2/results/ccdg_mesh_results.csv`（v3 行后缀 `_v3`；备份 `ccdg_mesh_results.csv.bak_wse_fix`） |
| v3 CCDG 产物 | `runs/pipeline/{short_4r_144747, short_16r_163455, short_64r_171510, long_16r_163513}/compact_v3.ccdg`（4 个，非折叠，保留完整交错结构） |

## 4. 实验结果

### 4.5 算力扫描（2026-08-26，wse_gating=0/free，目标计算占比 < 30% 即 blocked_ratio>0.70）

用新参数 `ccdg_compute_capability`（ops/s）扫描，从 1×=2.5e9 倍增（`CCDG_COMPUTE_CAP` 环境变量）；扫描脚本 `booksim2/cap_scan_free.sh`，产物带 `_cap<值>` 后缀（CSV/名称均区分，避免覆盖）。

| 负载 | mesh | ×1 | ×2 | ×4 | ×8 | ×16 | ×32 | ×64 | ×128 | 达标倍数 | 达标算力 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| short 4r | 2x2 | 79.4% | 77.0% | 72.5% | 64.9% | 51.8% | 36.3% | **24.6%** | 14.6% | ×64 | 1.6e11 ops/s |
| short 16r | 4x4 | 76.7% | 70.4% | 57.2% | 42.8% | **29.5%** | — | — | — | ×16 | 4e10 ops/s |
| short 64r | 8x8 | 63.7% | 57.9% | 42.7% | **28.9%** | — | — | — | — | ×8 | 2e10 ops/s |
| long 16r | 4x4 | 63.9% | 56.9% | 40.2% | **24.8%** | — | — | — | — | ×8 | 2e10 ops/s |

（表中为计算占比 = 1−blocked_ratio；达标 = 计算占比 < 30%）

- **rank 越多，达标所需算力越小**：4r 需 ×64，16r 需 ×16，64r 仅 ×8——通信占比基数随规模上升（4r 20.6% → 64r 36.3%），算力需求反向
- **blocked_cycles 随算力增加而下降**（如 4r：2.79M → 0.49M）：算力增强缩短关键路径计算段，rank 间到达通信点更同步，部分“暴露”被消化
- **通信地板浮现**：total_cycles 单调下降趋近地板（4r 150K、16r 216K、64r 149K、long16r 1.25M），即算力无限时的通信耗时下界
- 全部 unresolved=0，sent==recv；CSV 备份 `ccdg_mesh_results.csv.bak_cap_scan`

### 4.6 高算力下 WSE 门控对比（2026-08-26，short 64r × cap4e10=16×，w2）

算力扫描达标（×8）后再取 ×16 档（cap=4e10，free blocked_ratio=0.834，计算仅占 16.6%，强通信主导场景）验证 WSE 门控是否有收益拐点：

| 配置 | total_cycles | 相对 free | blocked_ratio | wse_inject_blocked |
|---|---:|---:|---:|---:|
| free | 129,587 | — | 83.4% | — |
| wse_p1_w2 | 156,070 | **+20.4%** | 86.0% | 3,727,124 |
| wse_p16_w2 | 154,125 | **+18.9%** | 86.0% | 3,840,528 |
| wse_p64_w2 | 167,576 | **+29.3%** | 87.0% | 4,134,912 |
| wse_p256_w2 | 182,935 | **+41.2%** | 87.8% | 4,407,040 |

**结论：通信主导（blocked_ratio 0.83）下 WSE 门控不仅无收益，性能税反而大幅加重**——×1 算力时税为 +3.6~6.8%（见 4.4），×16 算力时放大到 +18.9~41.2%。机制：高算力下计算段缩短，通信突发在时间轴上更集中（更陡峭），门控关断窗口挡住的比例更高 → 注入端排队拉长关键路径。相位宽度形状与 ×1 一致（p16 为最低点，p1/p16 接近，p64/p256 递增），但整体放大 ~5 倍。WSE 相位门控在 CCDG 突发型负载上无适用区间（与低算力结论一致，方向更强）。

产物：CSV `_cap4e10_1s_*` 5 行（free + p1/p16/p64/p256，备份 `ccdg_mesh_results.csv.bak_cap4e10_wse`）


### 4.1 v3 CCDG 规模与计算量守恒（dumpi2ccdg 侧）

| run 目录 | ranks | 节点数 | Σcompute_ops/rank | 折算 @2.5GHz | log 相位对照 |
|---|---:|---:|---:|---:|---|
| short_4r_144747 | 4 | 256 | 3.371M | 1.3486 ms | — |
| short_16r_163455 | 16 | 1,408 | 1.273M | 0.5093 ms | — |
| short_64r_171510 | 64 | 7,168 | 0.430M | 0.1719 ms | Pair=0.1497ms（22.62%），比值 ~1.15× |
| long_16r_163513 | 16 | 7,592 | 3.091M | 1.2363 ms | — |

- 64r short：Σcompute 折算 0.1719ms ≈ log Pair 相位 0.1497ms（偏差 ~15%，IPC=1 假设与窗口边界的合理残差）；旧版 ΣCOMPUTE ≈ Loop time（100%）的虚高已被消除
- log.lammps 64r short：Loop time=0.4239ms，Pair=22.62%，Comm=55.25%，Output=15.66%
- 单步非折叠节点数（64r 7,168）远小于旧折叠版，BookSim 秒级跑完

### 4.2 SimGrid 结构验证（v3-sim，仅结构判据，固定频率 SIMGRID_CPU_FREQ=2.8）

- **4/4 通过**：全活动执行（finish_time ≥ 0）、无死锁、无未 resolve 依赖、跨 rank 依赖链完整
- 验证产物：各 run 目录 `simgrid_dag/ccdg_dag_sim.cpp`（64r 版 29.5 万字节，非折叠 DAG）
- 注意：validate 不承担时间验证（角色边界），时间对照由 4.3/4.4 负责

### 4.3 BookSim free 基线 4 组（v3-run，1 步，全部 unresolved=0，sent==recv）

| run | mesh | total_cycles | 折算墙钟 | cycles/iter | compute_cycles | blocked_cycles | 计算占比 | 通信占比 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| short 4r | 2x2 | 3,395,750 | 1.698 ms | 3,395,750 | 10,788,457 | 2,791,611 | 79.4% | **20.6%** |
| short 16r | 4x4 | 1,329,947 | 0.665 ms | 1,329,947 | 16,298,542 | 4,939,136 | 76.7% | **23.3%** |
| short 64r | 8x8 | 544,482 | 0.272 ms | 544,482 | 22,007,212 | 12,517,158 | 63.7% | **36.3%** |
| long 16r | 4x4 | 3,871,384 | 1.936 ms | 3,871,384 | 39,562,885 | 22,311,017 | 63.9% | **36.1%** |

- blocked_ratio 定义：`blocked / (compute + blocked)`，即通信暴露时间占比（PE_BLOCKED 等依赖 resolve）
- 强扩展趋势：4r→16r→64r 通信占比 20.6%→23.3%→36.3%；long 16r（36.1%）≫ short 16r（23.3%），消息量驱动
- 与 log 口径：64r short BookSim 显式 36.3% vs log Comm 55.25%——残差 = 软件开销/握手（计划假设，不在仿真边界）
- 时间对照：v3 64r short 0.272ms 远低于旧 v2 的 0.62ms（v2 仍受均摊虚高影响，v3 纯间隙更真实）

### 4.4 WSE 复测（v3-wse，64r short × w2，门控 vs free）

| 配置 | total_cycles | 相对 free | blocked_ratio | wse_inject_blocked |
|---|---:|---:|---:|---:|
| free_v3（基线） | 544,482 | — | 36.3% | — |
| wse_p1_w2_v3 | 568,221 | +4.4% | 38.9% | 3,409,000 |
| wse_p16_w2_v3 | 563,850 | +3.6% | 38.4% | 3,487,376 |
| wse_p64_w2_v3 | 574,400 | +5.5% | 39.5% | 3,770,240 |
| wse_p256_w2_v3 | 581,370 | +6.8% | 39.8% | 3,980,032 |

**结论：v3 下 WSE 门控依然无收益拐点**——所有相位宽度均使 cycles 增加 3.6~6.8%，blocked_ratio 同步抬升（门控延后注包 → torn packets 增加 → 依赖等待变长）。与 v2 结论一致（WSE 相位门控在本负载是性能税）。

### 4.5 注入端反压（PE_BACKPRESSURE）建模验证（2026-08-26，compact_v3，free 1 步）

**背景**：原模型 packet 创建（PE 到通信节点即生成全部 flit，不查空间）与 flit 注入（credit 检查）完全解耦，用户指出真实硬件中注入 FIFO 满会反压到指令流，链路争用应通过反压体现到 CCDG。已实现：新增 `ccdg_inject_queue_depth`（flit 数，0=auto=最大单包 flit 数）；PE 在 SEND/集合节点生成前检查队列空间，不够则 `PE_BACKPRESSURE` 停等（计入 blocked_cycles，用户定口径：反压并入 blocked 统计）；注入腾出空间后重试。

| CCDG (v3) | depth(auto) | 反压触发次数 | unlimited cycles | auto cycles | Δcycles | auto blocked | unlimited blocked | Δblocked |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 4r short | 19,272 | — | 3,395,750 | 3,395,750 | 0 (0%) | 2,791,611 | 2,791,611 | 0 |
| 16r short | — | — | 1,329,947 | 1,329,947 | 0 (0%) | 4,939,136 | 4,939,136 | 0 |
| 64r short | 7,968 | 262,264 | 544,482 | 544,482 | 0 (0%) | 12,517,158 | 12,517,158 | 0 |
| 16r long | 22,032 | 514,997 | 3,871,384 | 3,864,172 | **-7,212 (-0.19%)** | 22,195,625 | 22,311,017 | -115,392 |

**核心结论（等待时间守恒）**：反压机制正常触发（64r 26 万次、16r long 51 万次，触发时队列 max 6,270/16,795 flit），但 total_cycles 不变（64r 0%、long -0.19% 且两次复现）。原因：CCDG 同步语义下网络拥塞的暴露等待**本来就通过接收端 WAIT 依赖建模**（包到不了对端 → WAIT 阻塞），注入端反压只是把同一段等待从"接收端 WAIT"转移到"发送端队列"——blocked 总量守恒（同一变量累计）。long 的 -0.19% 微改善来自集合展开包的注入时序摊平（credit 自适应节流 vs WSE 盲目门控）。

**意义**：① 用户担心的"争用不体现"不成立——争用已体现在 blocked_ratio（64r 36%）与 total_cycles 中；② 反压补充了指令流停顿的真实性（PE 确实在通信节点停等而非超前生成）；③ 对 total_cycles 指标不敏感，但为将来"队列深度=真实 FIFO 尺寸"的硬件校准提供参数口。

**验证方式**：debug 计数器（check_hits/state_cycles/max_queue_at_trigger）随仿真结束打印；unlimited 对照用 `CCDG_INJECT_QDEPTH=1000000000` 环境变量。

### 4.6 极限算力下反压 + WSE 门控对比（2026-08-27，short 64r × cap2.5e12=1000×，1 步，全 auto 反压）

**背景**：4.5 验证反压对 total 无影响后，用户要求测“加入 send 反压 + 极限算力（计算≈0，纯通信暴露）”下 free vs WSE 耗时。cap=2.5e12 ops/s（1000×默认），最大 COMPUTE 249,247 ops 折算 0.2 cycle，计算完全消失（compute_cycles 仅 23,675，blocked_ratio≈99.7%）。

| 配置 | total_cycles | 相对 free | 反压触发(check_hits) | wse_inject_blocked | blocked_cycles | blocked_ratio |
|---|---:|---:|---:|---:|---:|---:|
| free + auto | 125,305 | — | 902,904 | — | 7,972,623 | 99.7% |
| free + unlimited | 125,305 | 0% | 0 | — | 7,972,623 | 99.7% |
| wse_p16_w2 + auto | 135,613 | **+8.2%** | 297,776 | 3.88M | 7,792,653 | 99.7% |
| wse_p64_w2 + auto | 145,111 | **+15.8%** | 323,939 | 4.14M | 8,251,550 | 99.7% |
| wse_p256_w2 + auto | 158,803 | **+26.7%** | 321,267 | 4.41M | 8,983,321 | 99.7% |

**结论**：
1. **WSE 性能税随算力暴涨**：p256 从默认算力 +6.8%（4.4 节）→ 极限算力 +26.7%（≈4×）。机理：COMPUTE≈0 → 所有 rank 几乎同周期到达通信节点 → 注入波前完全同步 → 相位门控把整波流量挡在注入端，直接拉长关键路径；默认算力下 COMPUTE 提供自然异步节流，门控挡的流量少。
2. **反压守恒第三次成立（更强形式）**：free auto == free unlimited == 125,305 cycles 且 blocked_cycles 完全相同（7,972,623）——极限算力下反压触发 90 万次（3.4× 默认算力 26 万，同步性更强队列更易满）仍零差异，注入时间表由 credit 决定的结论不依赖算力。
3. **WSE 门控期间反压触发减少**（free 90 万 → wse ~30 万）：门控把 PE 挡在 PE_GATED，队列不会满，反压“无处发力”——两套节流机制互斥。
4. 物理含义：WSE 在“计算零成本”最坏场景下从“小税”变“大税”，再次验证 CCDG 负载下 WSE 相位门控与自然节流冲突、无收益拐点。

## 5. 实验问题记录

### 5.1 【已修复】CSV 汇总 blocked_cycles 列误填 bug（2026-08-26 发现）
- **现象**：`ccdg_mesh_results.csv` 中全部 8 个 v3 行的 `blocked_cycles` 列填成了 `compute_cycles` 值（如 64r free_v3 填 22,007,212，实际 12,517,158）
- **修复**：用 stats 文件真值重写 8 行；自洽校验通过（反推 compute == stats 的 compute_cycles 精确一致）
- **备份**：`ccdg_mesh_results.csv.bak_wse_fix`
- **教训**：CSV 汇总列必须与 stats 文件交叉校验；blocked_ratio 本身自洽（ratio = blocked/(compute+blocked)）可作反推校验手段

### 5.2 历史关键问题（v2 及更早，均已修复，勿回改）
- **BARRIER 锚定窗口裁剪**：迭代步窗口 = [最后 P2P 前最近 BARRIER, 其后最近 BARRIER]；勿用"最后一个非跳过节点"（会锚在 thermo ALLREDUCE 上，64r 把 halo P2P 全裁、争用低估 16.8%）
- **SimGrid DAG 根活动**：根 Comm 需前置 250-flops launcher Exec 并显式 start()，否则首计算块静默丢失
- **集合通信非阻塞模型**：PE 到达集体节点即展开注包并推进，只 BARRIER 保留全局同步；阻塞式必死锁
- **dumpi2ccdg eliminate_cycles**：集合通信超节点化后迭代 DFS 删环上 cross edge

### 5.3 已知口径/边界（报告时必须说明）
- 软件开销不建模：发送方握手延迟、MPI 协议处理不在仿真范围 → 总时间残差为正且随消息数增长
- 计算量反推假设 1 op/cycle（IPC=1）；若守恒检查系统性偏差可用 log Pair 相位反推校准 IPC 系数（当前 64r 偏差 ~15%）
- blocked_cycles 是 PE 累计口径（64 PE 求和），total_sim_cycles 是全局时间线，两者不可直接比较

### 5.4 【已修复】run_ccdg_mesh.sh 文件命名不含队列深度导致 auto/unlimited 同名覆盖（2026-08-27 发现）
- **现象**：`CCDG_INJECT_QDEPTH=0`(auto) 与 `=1000000000`(unlimited) 跑同一算力/门控组合时 cfg/log/stats 同名，后跑者覆盖前者 log（CSV 两行都在但 debug 计数丢失）
- **修复**：脚本 TAG 追加 `_qd<值>` 标识（仅非默认 0 时），unlimited 产物名如 `..._cap2.5e12_qd1000000000_1s_free`
- **教训**：run_ccdg_mesh.sh 的任何非默认参数都必须体现在文件名中（算力已有 CAP_TAG，队列深度是本次补的）

## 6. 未做/可选项（将来可能的延续）

- 算力扫描（可选）：`ccdg_compute_rate` 扫描 0.5×/1×/2× CPU 水平，考察 WSE 算力配置对 blocked_ratio 与总性能的影响（计划第 6 项，未执行）
- 64r short 的 w4 门控 v3 版未跑（v2 时代跑过 w4，v3 只跑了 w2）
- 多步场景：v3 未折叠，多步用 `ccdg_unroll.py` 展开（单步 64r ~7k 节点，展开成本可控）

## 7. 常用命令速查

```bash
# 重新生成 v3 CCDG（不折叠）
cd /work1/jiangtao/lammps_trace/runs/pipeline/<dir> && \
LD_LIBRARY_PATH=/work1/jiangtao/lammps_trace/install/lib \
CCDG_TRIM_SETUP=1 /work1/jiangtao/lammps_trace/dumpi2ccdg/dumpi2ccdg .

# LEGACY 回归（应 == 旧 compact_v2）
CCDG_LEGACY_CPU_MID=1 CCDG_TRIM_SETUP=1 .../dumpi2ccdg .

# 注入 BookSim（v3：cfg 需含 ccdg_compute_capability=2.5e9，或旧参数 ccdg_compute_rate=1.25）
cd /work1/jiangtao/lammps_trace/booksim2
./run_ccdg_mesh.sh /work1/jiangtao/lammps_trace/runs/pipeline/<dir>/compact_v3.ccdg 3600 1
# 算力扫描：CCDG_COMPUTE_CAP=1.25e9(0.5×)/2.5e9(1×)/5e9(2×) ./run_ccdg_mesh.sh ...

# SimGrid 结构验证（固定频率仅结构判据）
python3 /work1/jiangtao/lammps_trace/validate_simgrid.py <run_dir> <num_ranks> compact_v3.ccdg

# 查看 v3 统计（compute/blocked）
grep -E "compute_cycles|blocked_cycles|blocked_ratio" .../*_v3_stats.txt
```
