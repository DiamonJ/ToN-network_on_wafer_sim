这个重构方向很清晰——从“对 MPI trace 做后处理编译”升级为“从 LAMMPS 源头直接编译成 WSE 程序、由贴近 Cerebras 硬件的执行器回放”。我基于论文机制细节（§III-B/F、Fig 3-6）和现有代码资产制定如下计划。

## 总体架构

```
┌────────────────┐   ┌──────────────────┐   ┌─────────────────┐   ┌──────────────┐
│ LAMMPS 源码     │   │ WSE_compiler     │   │ BookSim         │   │ 评估         │
│ + wse_plan     │ ─►│ (Python, 编译器)  │ ─►│ WSETrafficMgr   │ ─►│ evaluation   │
│ 发射器 (C++)    │   │                  │   │ (C++, 新增类)    │   │ .txt/.json   │
└────────────────┘   └──────────────────┘   └─────────────────┘   └──────────────┘
  每相位的精确通信计划    MD-IR→放置→stage→     wavelet 多播注入、       cycles/iter
  (面/字节/步数/几何)     相位/波前/归约编译     路由器状态机、VC         per-stage 分解
       │                + 冲突闭包+est          + 命令波let             链路利用率
       ▼                      ▲
  DUMPI trace（降级为校准/对拍 oracle，不再是编译器输入）
```

关键架构决策：**编译器输入从“MPI trace”换成“LAMMPS 源码级通信计划”**。MPI 运行时痕迹（同步、skew、busy-wait）在源头就不引入，而不是像现在这样捕获后再剥离（trim→demand→HB 三层补丁的根源）。

## Phase 0：LAMMPS wse_plan 发射器（~1.5 天）

> 状态：CommBrick short-range MVP 已实现并接入 `run_noc_pipeline.sh`。16r
> LiAlOCl、4r H2O、4r Cu/EAM 的 run 段逐方向消息数和字节数与 trimonly
> CCDG 完全一致。long 可运行，但当前只导出 CommBrick，Kspace/FFT 标为 PARTIAL。

LAMMPS 在 `comm_brick setup` 阶段已经算出了 swap 拓扑、邻居、ghost 宽度和
slab 边界；**精确的 sendlist 尺寸要到 `borders()` 根据原子位置建表后才知道**。
实现使用 env 门控 `LAMMPS_WSE_PLAN=<前缀>`，每 rank 独立写
`<前缀>.rankNNNN.jsonl`，不增加 MPI 调用或共享文件同步；LAMMPS 退出后由
`merge_wse_plan.py` 合并成 `wse_plan.json`：

| 相位 | 源码位置 | 导出内容 |
|---|---|---|
| setup/borders/forward/reverse/Pair comm | `comm_brick.cpp` | 每面 (dir, dst_proc, nsend, packed values, bytes)，swap 维度序及 setup/run scope |
| kspace grid swap | `grid3d.cpp` | 每面 swap 轮数与宽度 |
| FFT remap | `remap.cpp` | 收发对集合（long 阶段） |
| allreduce/thermo | `pppm.cpp`/`output.cpp` | 操作数、字节数 |
| compute | `verlet.cpp` | 每相位间 ops 估计（用现有 phase_trace 的墙钟差折算） |

**验收**：`validate_wse_plan.py` 对同一算例的 run-scope wse_plan 与 trimonly
CCDG 做逐方向消息数/字节数比较，偏差 ≤2%；DUMPI 已降级为对拍 oracle。

## Phase 1：WSE_compiler（~4 天，重度复用现有件）

输入 `wse_plan.json` + 配置，输出 **WSE 程序**（wavelet 调度 + est 表）+ 编译报告。模块划分：

1. **放置**：fold-PBC 交叉排布内建（论文 III-E），Z 压缩天然成立（编译器自己排 worker grid，不再依赖 LAMMPS 处理器网格）；
2. **stage 编译器**：每通信相位 → H/V 两 stage；b 由几何直接算出（`2C+rcut` 界的显式版），不再从 trace 方向直方图反推；
3. **相位/条带调度**：(b+1)×ny 竖条、b+1 相位、行进顺序——`ccdg_demand.py` 的 cb_phase_open/cb_remaining 机制直接迁移；
4. **波前预留**：LinkTable 升级为**树状足迹**——一条 multicast 波前按论文语义占链路（body 核一收两发，共享链路只计一次），这是修复 fold 反向发现（单播投影 b=2 成本翻倍）的核心；
5. **归约编译器**：reverse_comm → 论文附录的“逆多播 2:1 分支归约”需求类型；
6. **HB/断言链全保留**：零违例、同相位零冲突、est 单调、消息守恒。

**验收**：16r/256r short 编译通过全断言；est 与下游 BookSim 实测偏差 ≤1%。

## Phase 2：BookSim WSETrafficManager（v1 ~4 天，v2 可选 +1.5 周）

新增 `sim_type=wse`，与 CCDGTrafficManager 并存（五档基线不动）。贴近 WSE 硬件的参数口径：`flit_size_bytes=4`（32-bit wavelet）、4 VC（每 stage ±方向各一）、每 hop ~1 cycle 的浅流水 profile（与现 IQ 4-cycle profile 做敏感性对照）、命令波let 建模为单 flit 零载荷包。

**多播分两级实现（本计划最大的工作量决策）**：
- **v1（推荐先做）**：traffic-manager 级多播——头核一次注入，manager 按编译器足迹展开分支包；冲突闭包由编译器保证（BookSim 只回放），congestion≈0 by construction——这本身就是对论文“确定性数据流”主张的忠实复现；
- **v2（可选）**：路由器级分支——`router.cpp`/`flit.cpp` 改造，头 flit 在分支点复制、credit 分裂，命令波let 动态驱动 head/body/tail 行进（est 表退化为正确性备份）。真正的动态多播，但动 BookSim 网络核心，风险和工作量都高一档。

**验收**：注入波前数 == 送达分支数；v1 下 congestion ≤0.1%；v2 下与 v1 cycles 偏差 ≤5%（相互验证）。

## Phase 3：评估与实验矩阵（~3 天）

- 指标：cycles/iter、steps/s、**per-stage 分解**（exchange/borders/reverse 各占多少——论文性能模型的 6ns/candidate 口径可对齐）、链路利用率热图、波前计数；
- 实验矩阵：{16, 64, 256}×{short}×{WSE-fast, IQ}×{fold on/off}——**fold on 现在应该兑现论文 "PBC is free"**，直接检验我们对 fold 反向发现的诊断；
- 基线对照：同算例五档（free/hb/cerebras/...），"编译式 WSE vs trace 驱动 demand”的差值就是新架构的净收益。

## Phase 4：文档（~0.5 天）

`WSE_compiler_design.md` + SKILL.md 新入口 + workflow md 归档五档为历史口径。

## 风险与边界

1. **long/PPPM 超出论文范围**：论文是纯短程 EAM，没有 FFT。计划里 kspace 网格 swap 编成波前没问题，但 **FFT remap 的全排列通信没有论文机制可循**——v1 先做 short，long 作为“WSE 机制的边界实验”单独处理；
2. **v2 路由器改造可能引入死锁/credit 一致性问题**——这是把 v2 设为可选的原因；
3. compute 模型精度：WSE 核算力参数无公开精确值，用敏感性扫描代替单点。

## 三个需要你拍板的点

1. **多播深度**：v1（manager 级、编译保证无冲突）先行，v2（路由器级分支）验证后再上？我推荐是；
2. **kspace 范围**：第一版只做 short（对齐论文能力域），long 留第二阶段？
3. **五档基线保留**：作为对照基线不删（同源对比的锚点）？

确认后从 Phase 0 的 LAMMPS 发射器开工，总计 v1 端到端约 2 周。