# LAMMPS Trace 工程：代码归属与实验实践指南

> 工程根目录：`/mlx_devbox/users/liuyicheng.1/playground/dpmd`  
> 推荐入口：`README_REPRODUCE.md`、`run_noc_pipeline.sh`  
> 本文目标：快速区分代码来源，并能独立完成主体实验、检查产物。

## 1. 工程解决什么问题

工程实现以下链路：

```text
LAMMPS MPI 计算
  → DUMPI 捕获 MPI trace
  → dumpi2ccdg 生成通信—计算依赖图（CCDG）
  → 需求驱动编译 / 论文机制映射
  → BookSim 2D mesh NoC 仿真
  → 同源五档结果比较
```

论文机制主要参考 Santos 等人在 SC 2024 的 wafer-scale MD 工作，包括
PBC fold、X/Y stage、barrier、b+1 wavelet phase 等。这里不是原论文代码，
而是作者在 LAMMPS trace 和 BookSim 上完成的单播投影复现。

## 2. 哪些是下载的仓库，哪些是作者实现

### 2.1 第三方上游源码

| 路径 | 来源 | 在本工程中的作用 |
|---|---|---|
| `lammps-src/` | LAMMPS 29Aug2024-Update1 源码快照 | 执行分子动力学计算 |
| `sst-dumpi/` | SST DUMPI 16.x 源码快照 | `libdumpi` 捕获 MPI；`libundumpi` 读取 trace |
| `booksim2/` 的原有 tracked 文件 | [BookSim 2.0](https://github.com/booksim/booksim2) | 周期精确 NoC 仿真基础设施 |

判断依据：

- `booksim2/.git` 的 `origin` 指向官方 BookSim 仓库。
- LAMMPS、DUMPI 目录保留各自上游版权和许可证，但未保留独立 git 元数据。
- `install/` 是这些组件的本地构建安装目录，不是源代码。

### 2.2 在第三方源码上的必要适配

| 路径 | 改动 |
|---|---|
| `lammps-src/src/timer.{h,cpp}` | 增加 `LAMMPS_PHASE_TRACE` 和 phase marker，供 trace 与 LAMMPS 子阶段对齐 |
| `lammps-src/src/KSPACE/pppm.cpp` | 标记 GRID_REVERSE、FFT、GRID_FORWARD、FORCE、REDUCE 子阶段 |
| `lammps-src/src/KSPACE/remap.cpp` | 标记 FFT 通信和 FFT 计算阶段 |
| `booksim2/src/booksim_config.cpp` | 增加 CCDG、计算能力、schedule、WSE 等配置字段 |
| `booksim2/src/main.cpp`、`trafficmanager.{hpp,cpp}` | 注册 CCDG 仿真类型并接入 WSE 注入门控 |

这些改动负责“接得上”：让 LAMMPS 可被分阶段观测，让 BookSim 能接收 CCDG
及论文机制参数。LAMMPS 的其余主体和 BookSim 路由器、拓扑等主体仍是上游代码。

### 2.3 作者自研的核心实现

| 路径 | 类型 | 作用 |
|---|---|---|
| `dumpi2ccdg/dumpi2ccdg.cpp` | 数据转换 | DUMPI → CCDG；生成依赖边、计算间隙、BARRIER 锚定裁剪和 compact |
| `booksim2/src/ccdg_trafficmanager.{hpp,cpp}` | BookSim 核心扩展 | 解析 CCDG，维护每个 rank 的 PE 状态，按依赖发送和唤醒 |
| `booksim2/ccdg_demand.py` | 论文机制编译器 | 提炼通信需求；实现 HB、fold-PBC、Cerebras 和 interleave 档 |
| `booksim2/ccdg_planner.py` | 规划器 | 生成链路时槽和确定性通信计划 |
| `booksim2/ccdg_scheduler.py` | 调度器 | 生成 earliest-start-time（EST）调度 |
| `booksim2/run_ccdg_mesh.sh` | 注入脚本 | 根据 rank 数生成方形 mesh 配置并运行 BookSim |
| `run_noc_pipeline.sh` | 当前主流水线 | 捕获、转换、质量检查、五档注入和评估 |
| `run_lmp_ccdg.sh` | 旧流水线 | CCDG + SimGrid 验证，可选 BookSim |
| `ccdg_unroll.py` | 工具 | 将单步 CCDG 展开为多步 CCDG |
| `ccdg_simgrid_validate.py` | 工具 | 历史 SimGrid DAG 验证入口 |
| `traffic_pattern/` | 分析工具 | 分析通信模式以及 Kspace phase |
| `cases/` | 实验输入 | LiAlOCl、H2O、Cu/EAM、LJ 等算例 |

其中真正对应论文机制的代码主要是：

1. `ccdg_demand.py` 中的 fold-PBC、HB、X/Y stage、stage barrier 和 wavelet 编译；
2. `trafficmanager.*` 中的 WSE/wavelet 注入门控；
3. `ccdg_trafficmanager.*` 中对确定性 schedule 和 CCDG 依赖的执行；
4. `ccdg_planner.py`、`ccdg_scheduler.py` 中的计划与时钟安排。

> 归属说明：整包没有顶层 git 历史，无法精确归属到个人。本文的“作者自研”
> 指复现包相对上述三个上游项目新增或修改的项目代码。

## 3. 主体实验概览

| 优先级 | 实验 | 目的 | 当前状态 |
|---|---|---|---|
| 主实验 | LAMMPS → CCDG → BookSim 五档同源比较 | 验证需求驱动及论文机制对 NoC makespan 的影响 | 当前推荐 |
| 支撑实验 | Kspace / FFT 强扩展分析 | 解释 long-range 场景的全局通信瓶颈 | 已完成，可选复现 |
| 历史验证 | CCDG → SimGrid DAG | 验证 CCDG 时间建模误差 | 保留但不再作为主闸门 |
| 历史专项 | CCDG v3、算力扫描、WSE 门控 | 分析纯间隙建模、算力和注入门控 | 结论归档，非必跑 |

实践时先完成主实验。只有需要验证研究过程或追溯结论时，再运行其他实验。

## 4. 一次性构建与环境验证

依赖：GNU Make、CMake、g++、Python 3、OpenMPI。

最简方式：

```bash
cd /mlx_devbox/users/liuyicheng.1/playground/dpmd
bash smoke_test.sh
```

该脚本会按需完成：

1. 构建并安装 DUMPI 到 `install/`；
2. 构建 `dumpi2ccdg`；
3. 构建 BookSim；
4. 构建带 KSPACE/MANYBODY 的 LAMMPS；
5. 用附带 CCDG 做一次 BookSim 冒烟注入；
6. 运行一次 16-rank 完整主流水线。

主要构建产物：

```text
install/bin/lmp
install/lib/libdumpi.so
install/lib/libundumpi.so
dumpi2ccdg/dumpi2ccdg
booksim2/src/booksim
```

如需逐项手工构建，按 `README_REPRODUCE.md` 第 3 节执行。

## 5. 主实验：五档 NoC 同源比较

### 5.1 推荐命令

```bash
cd /mlx_devbox/users/liuyicheng.1/playground/dpmd

# 快速主算例
./run_noc_pipeline.sh short 16 lialocl 2688 both

# 大规模对比
./run_noc_pipeline.sh short 256 lialocl 2688 both

# 含 PPPM/Kspace 的长程作用
./run_noc_pipeline.sh long 16 lialocl 2688 both
```

参数格式：

```text
./run_noc_pipeline.sh <short|long> <rank数> <cu|h2o|lialocl> <目标原子数> [free|demand|both]
```

- `short`：截断库仑作用；`long`：启用 PPPM/Kspace。
- rank 数必须是完全平方数，如 4、9、16、25、64、256。
- `both` 是推荐模式，运行全部五档。
- 脚本强制 `processors K K 1`，将 rank 映射到 `K×K` 的 2D mesh。

### 5.2 每一步操作和产物

一次运行只捕获一次真实 LAMMPS，随后所有档位共用该次捕获。

| 步骤 | 操作 | 主要输入 | 主要产物 |
|---|---|---|---|
| 1 | 按体系、原子数、rank 生成输入 | 命令行参数、`cases/` | `in.lammps`、所需 `data.*`/势文件 |
| 2 | `mpirun` 执行 LAMMPS，并用 `LD_PRELOAD` 捕获 DUMPI | `lmp`、`libdumpi.so` | `lammps.log`、`log.lammps`、`dumpi-*.bin/.meta` |
| 3a | 转换完整 trace | DUMPI 文件 | `trace_<N>ranks_global.ccdg`、`ccdg_gen.log` |
| 3b | 裁剪 setup 并折叠 burst | DUMPI 文件 | `compact_<N>ranks_global.ccdg`、转换日志 |
| 3c | 只裁剪 setup，不折叠 | DUMPI 文件 | `trimonly_<N>ranks_global.ccdg`、`ccdg_trimonly_gen.log` |
| 4 | 执行数据质量闸门 | trimonly、LAMMPS 日志 | `quality_gate.txt` |
| 5 | 编译并注入五档 BookSim | trimonly、demand compiler | 五个 `booksim_*` 目录、plan、est、stats、result |
| 6 | 汇总结果并判定 | 各档 stats/log | `evaluation.txt`、`evaluation.json` |

运行目录为：

```text
runs/pipeline/<short|long>_<体系>_<原子数>a_<rank数>r_<时间戳>/
```

### 5.3 五档分别表示什么

| 档位 | 载体/机制 | 要回答的问题 |
|---|---|---|
| `free` | trimonly trace 自由注入，保留同步 | 原始反应式执行基线 |
| `free_fold` | free + paper III-E PBC fold 重标号 | fold 单独带来什么影响 |
| `hb` | fold + demand + happens-before | 正确性约束下，需求驱动编译是否有收益 |
| `cerebras` | hb + X/Y stage + barrier + b+1 串行轮转 | 论文式 stage/wavelet 机制的完整代价 |
| `ilv` | cerebras，但 wavelet phase 交错 | 串行轮转本身贡献多少代价 |

有效比较关系：

```text
free      → free_fold ：PBC fold 对反应式执行的影响
free_fold → hb        ：需求驱动编译的净收益
hb        → cerebras  ：stage 串行化和 barrier 的代价
cerebras  → ilv       ：相位串行与交错的差异
```

不要跨不同 run 目录比较，因为不同捕获批次的节点数、包数和计算时间可能不同。

### 5.4 如何验收结果

先查看：

```bash
cat runs/pipeline/<本次目录>/quality_gate.txt
cat runs/pipeline/<本次目录>/evaluation.txt
```

合格条件：

1. BARRIER 锚定覆盖 `ranks/ranks`，通信字节守恒；
2. processor grid 为 `K by K by 1`；
3. 通信方向仅包含面邻居和 PBC 接缝；
4. 每档 `unresolved == 0`；
5. 每档 `packets_sent == packets_recv`；
6. HB 违例和 stage inversion 为 0；
7. 估计 makespan 与 BookSim 实测偏差不超过 1%；
8. `evaluation.txt` 末尾为 `OVERALL: PASS`。

附带样例位于：

```text
runs/pipeline/short_lialocl_2688a_16r_20260831_195028/
```

其五档均通过；可先阅读其中的 `quality_gate.txt` 和 `evaluation.txt`。

## 6. 不重跑 LAMMPS，只实践 BookSim

使用附带的 trimonly CCDG：

```bash
cd /mlx_devbox/users/liuyicheng.1/playground/dpmd/booksim2
CCDG_COMPUTE_CAP=2.5e10 ./run_ccdg_mesh.sh \
  ../runs/pipeline/short_lialocl_2688a_16r_20260831_195028/trimonly_16ranks_global.ccdg \
  600 1
```

产物写入 `booksim2/results/`，包括动态生成的 cfg、BookSim log、stats 和汇总 CSV。
这是最快的代码走读和调参入口。

## 7. 其他实验如何操作

### 7.1 历史 SimGrid CCDG 验证

```bash
./run_short_lmp.sh 16 lialocl 2688 10 none
./run_long_lmp.sh 16 lialocl 2688 10 none
```

额外产物：

```text
simgrid_dag/
validation_result.txt
```

其目标是比较 CCDG 模拟时间和真实 LAMMPS `Loop time`，历史通过阈值为误差不超过
5%。256 rank 强扩展时自动校准频率会失去物理意义，因此当前主流水线已改用
`quality_gate.txt`，不应再把 SimGrid 当作大规模实验的主验收标准。

### 7.2 Kspace / FFT 支撑实验

该实验使用 SMPI 版 LAMMPS和 2D mesh platform，分析 PPPM 中 FFT 计算与转置通信：

```text
LAMMPS_PHASE_TRACE=phase.csv
  + smpirun
  + traffic_pattern/analyze_phase.py
```

主要产物是 phase CSV、SMPI log 和分析结果。完整环境、命令和历史结论见
`Kspace_FFT实验手册.md`。复现包未携带完整 `lammps-build-smpi/`，需要先按手册
另行构建，因此它不是首次实践的推荐入口。

### 7.3 CCDG v3 / 算力扫描 / WSE 门控

这是归档专项，主要研究：

- 纯计算间隙建模；
- `CCDG_COMPUTE_CAP` 算力扫描；
- WSE phase gating；
- `CCDG_INJECT_QDEPTH` 注入反压。

操作和既有结论见 `CCDG_v3_实验交接记录_20260826.md`。除非要复核历史结论，
否则优先使用当前五档主流水线。

## 8. 实践时最容易踩的坑

1. `dumpi2ccdg` 对 `CCDG_COMPACT` 只检查变量是否存在；生成 trimonly 时必须：

   ```bash
   env -u CCDG_COMPACT ...
   ```

2. `CCDG_COMPUTE_RATE` 必须写成浮点数，例如 `0.0`，不要写整数 `0`。
3. LAMMPS log 可能有两组 timing breakdown；第二组才是 MD run。
4. 同一目录放多个 CCDG 可能导致 BookSim 结果 TAG 相互覆盖。
5. 跨 run、跨载体比较没有统计意义；只解释同一次捕获中的五档结果。
6. 不带 HB 的旧 demand 档会删除等待语义，属于乐观下界；正式结论使用 `hb`。
7. 单播 BookSim 中的 fold-PBC 不等同于论文的硬件多播，因此不能直接宣称完整复现
   “PBC is free”。

## 9. 建议阅读顺序

1. 本文；
2. `README_REPRODUCE.md`；
3. 样例 run 的 `quality_gate.txt` 和 `evaluation.txt`；
4. `run_noc_pipeline.sh`；
5. `dumpi2ccdg/dumpi2ccdg.cpp`；
6. `booksim2/ccdg_demand.py`；
7. `booksim2/src/ccdg_trafficmanager.cpp`；
8. 需要研究背景时再读 `booksim2/ccdg_demand_workflow.md` 和各历史实验记录。
