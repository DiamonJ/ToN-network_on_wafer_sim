# 静态 T1/C1 估算器说明

工具：`estimate_lammps_cost.py`  
模型名：`static_uniform_density_v1`  
输出：`static_cost_estimate.json`

本文只依赖本文件与估算器源码即可读懂：**输入是什么、估的是什么、每个变量含义、公式怎么算、为什么这么算**。

---

## 1. 一句话：算的是什么

给定一个 **LAMMPS 输入脚本**（以及它引用的 data / 势文件），在 **不真正跑 MPI 仿真** 的前提下，估算每个 MPI rank 在 **一个 Verlet timestep** 上的：

| 量 | 符号 | 单位 | 含义 |
|---|---|---|---|
| 通信成本 | **T1** | bytes / rank / step | 该 rank 作为发送端发出的 **应用层 payload 字节数**（不含 MPI 头、集合通信） |
| 计算成本 | **C1** | DP-equivalent ops / rank / step | 该 rank 上积分 + 邻居遍历 + 力计算（+ 可选 PPPM）的 **双精度算术等价操作数** |

注意：

- T1 **不是** 墙钟时间，也不是 NoC 上的 hop×bytes；它是“发了多少有效数据”。
- C1 是按固定系数估计的双精度算术工作量，可与 lane-weighted
  `FP_ARITH_INST_RETIRED` 对拍；它不是指令数、CPU cycles 或墙钟时间。
- 默认口径是 **稳态步**（不重建邻居表）。重建步另给 `T1_rebuild_*`，但 **不算原子迁移 `exchange`**。

---

## 2. 怎么用

```bash
python3 estimate_lammps_cost.py in.lammps -o static_cost_estimate.json
```

`run_noc_pipeline.sh` 也会自动调用，并在运行目录写出同名 JSON。

### 2.1 当前支持的输入形态

| 约束 | 取值 |
|---|---|
| `units` | `metal` |
| `boundary` | `p p p`（三维周期） |
| `processors` | `Px Py Pz`，显式给出 |
| `atom_style` | `atomic` 或 `charge` |
| 构型来源 | `read_data`（可再 `replicate`），或 `lattice fcc` + `region box block` + `create_atoms` |
| `pair_style` | `eam`；`lj/cut/coul/cut`；`lj/cut/coul/long` + `kspace_style pppm` |
| 其它 | pipeline 常用的 `group` / `set type` / `set charge` |

不支持的输入会直接报错，不会静默给出错误结果。

---

## 3. 输入：从脚本读出什么

估算器解析 `in.lammps`（及 data 文件）后得到系统描述。下表是后续公式用到的 **原始输入**。

| 符号 | 代码/字段 | 来源 | 含义 |
|---|---|---|---|
| \(N\) | `input.atoms` | data / FCC 生成后（含 replicate） | 全局原子数 |
| \(\mathbf{r}_i=(x_i,y_i,z_i)\) | `system["atoms"]` | 每个原子坐标 | 静态构型位置（积分前） |
| \(q_i\) | atom charge | `atom_style charge` + `set` | 电荷；仅 PPPM / 部分解析需要 |
| \(L_x,L_y,L_z\) | `input.box` | 盒子上界−下界 | 全局正交盒子边长 |
| \(P_x,P_y,P_z\) | `input.procgrid` | `processors` | 三维处理器网格 |
| \(R\) | `input.num_ranks` | \(P_x P_y P_z\) | MPI rank 数 |
| \(r_c\) | `input.force_cutoff` | pair cutoff（EAM 从势文件读） | 力计算截断半径 |
| \(s\) | `input.neighbor_skin` | `neighbor` 命令；缺省 2.0 | 邻居表皮肤厚度 |
| \(\alpha\) | `input.kspace_accuracy` | `kspace_style pppm <acc>` | PPPM 精度；无 kspace 则为空 |
| atom style | `input.atom_style` | `atom_style` | 决定 borders 每原子打包宽度 |
| pair style | `input.pair_style` | `pair_style` | 决定力内核与是否有 EAM 额外通信 |

### 3.1 由输入直接导出的量

| 符号 | 公式 | 含义 |
|---|---|---|
| \(\rho\) | \(N/(L_x L_y L_z)\) | 全局平均数密度 |
| \(l_d\) | \(L_d/P_d\)，\(d\in\{x,y,z\}\) | 均匀分解下每 rank 子域边长 |
| \(g\) | \(r_c+s\) | **ghost 通信宽度**（邻居 cutoff） |
| \(N_{\mathrm{local}}(r)\) | 按坐标落入哪个子域计数 | rank \(r\) 拥有的 owned 原子数 |
| \(\mathrm{scale}(r)\) | \(N_{\mathrm{local}}(r)\,/\,(N/R)\) | 相对平均负载的缩放因子 |

**为什么用静态坐标：**  
borders / forward 的 sendlist 由几何 slab 决定，初始构型足以重放“谁该发给谁”。  
**为什么不算 exchange：**  
原子迁移取决于积分后是否跨子域边界，静态输入没有速度演化，无法可靠得到 \(N_{\mathrm{migrate}}\)。

---

## 4. 总体计算流程

```text
in.lammps (+ data)
    │
    ▼
解析系统：N, 坐标, L_d, P_d, r_c, s, atom/pair/kspace
    │
    ├─► 按均匀子域给每个原子分到 owned rank → N_local(r)
    │
    ├─► 坐标重放 CommBrick::borders() → N_copy(r)     【通信几何，精确重放】
    │
    ├─► 均匀密度估计 N_list, N_pair, N_ghost           【计算量近似】
    │
    ├─► (可选) PPPM 网格尺寸 + grid halo + FFT remap
    │
    ▼
每个 rank：
    T1_steady  = forward + reverse + (EAM extra) + (PPPM terms)
    T1_rebuild = borders + reverse + (EAM extra) + (PPPM terms)
    C1         = integrate + pair/EAM + (PPPM ops)   【取 min/max 中点】
```

设计意图：

1. **通信（CommBrick）尽量“可复现”**：用真实原子坐标重放 borders，而不是 \(\rho\times\) 体积瞎估。
2. **计算用均匀密度闭式近似**：邻居表/力对数无法在不建邻接表时精确得到。
3. **只估应用 payload**：方便和 CCDG / WSE plan 的字节守恒对齐。

---

## 5. 通信几何：\(N_{\mathrm{copy}}(r)\)

### 5.1 它表示什么

\(N_{\mathrm{copy}}(r)\) = rank \(r\) 在一次完整 **borders** 过程中，向 **其它 rank** 发送的 **原子副本次数**（同一原子可在多轮/多维被多次计入）。

这是后续 forward / reverse / borders / EAM 额外通信的共同乘数。

### 5.2 怎么得到（`commbrick_copies`）

对每个维度 \(d=0,1,2\)（X→Y→Z）：

1. 传播轮数（与 LAMMPS `maxneed` 一致）：

\[
m_d=\left\lfloor\frac{g\cdot P_d}{L_d}\right\rfloor+1
=\left\lfloor\frac{g}{l_d}\right\rfloor+1
\]

若 \(P_d=1\)，该维 **不产生网络拷贝**（本地复制，如 `processors K K 1` 的 Z 维）。

2. 对该维做 \(2m_d\) 个 swap（负向、正向交替）。  
   每个 swap 定义发送 slab \([\texttt{slab\_lo},\texttt{slab\_hi}]\)，发给面邻居；若在周期缝上，坐标加减 \(L_d\)。

3. 只有落在 slab 内的原子（含上一轮刚收到、进入本 rank `present` 列表的 ghost）才会被打包转发。

4. 若目的 rank ≠ 本 rank，则 \(N_{\mathrm{copy}}(r)\;{+}=1\)（按原子计）。

| 符号 | 含义 |
|---|---|
| \(m_d\) | 维度 \(d\) 上最多跨几个邻居子域 |
| slab | 本轮要发送的边界薄片坐标范围 |
| \(g\) | slab 厚度上界（ghost width） |
| PBC shift | 周期镜像：\(\pm L_d\) |

**为什么这样算：**  
这与 `CommBrick::setup()` + `borders()` 的维序、正负向、多跳接力一致；short 场景下与真实 CommBrick 发送原子数可对齐到很高精度。

### 5.3 辅助几何体积（`swap_geometry`）

输出里还有密度公式下的体积明细，用于对照，**最终 T1 不直接用它**，而用坐标重放的 \(N_{\mathrm{copy}}\)：

\[
h_{d,m}=\max\bigl(0,\min(l_d,\,g-m\,l_d)\bigr)
\]

\[
A_d=\Bigl(\prod_{j<d}\min(L_j,l_j+2g)\Bigr)\Bigl(\prod_{j>d}l_j\Bigr)
\]

\[
V_{d,m}=2\,h_{d,m}\,A_d
\]

| 符号 | 含义 |
|---|---|
| \(h_{d,m}\) | 第 \(m\) 轮有效 slab 厚度 |
| \(A_d\) | 做完前面维度通信后的有效截面积 |
| \(V_{d,m}\) | 该维该轮正负两个方向的近似发送体积 |

---

## 6. 通信字节 T1

一律：

\[
\text{bytes}=(\text{原子副本数})\times(\text{每原子 doubles 数})\times 8
\]

其中 \(8\) = 一个 `double` 的字节数。

### 6.1 各通道公式

设 \(N_c=N_{\mathrm{copy}}(r)\)。

| 通道 | 公式 | 每原子 doubles | 为什么 |
|---|---|---|---|
| forward | \(T_{\mathrm{fwd}}=N_c\times 3\times 8\) | 3（\(x,y,z\)） | 每步把 owned 坐标更新到 ghost |
| reverse | \(T_{\mathrm{rev}}=N_c\times 3\times 8\) | 3（力分量） | `newton on` 时 ghost 侧力回传给 owner |
| borders | \(T_{\mathrm{bord}}=N_c\times S_{\mathrm{bord}}\times 8\) | atomic: 6；charge: 7 | 重建邻居时建立 ghost，打包比 forward 更宽 |
| EAM extra | \(T_{\mathrm{EAM}}=N_c\times 2\times 8\) | 2 | EAM 额外 pair forward+reverse；非 EAM 为 0 |

### 6.2 稳态步与重建步

**稳态步**（不重建邻居，复用已有 sendlist）：

\[
T_{\mathrm{steady}}(r)
=T_{\mathrm{fwd}}+T_{\mathrm{rev}}+T_{\mathrm{EAM}}
+T_{\mathrm{grid\text{-}rev}}+T_{\mathrm{grid\text{-}fwd}}+T_{\mathrm{FFT}}
\]

对应输出：`T1_steady_send_bytes`（历史字段 `T1_send_bytes` 同值）。

**重建步**（`borders` 重建 sendlist；**不含 exchange**）：

\[
T_{\mathrm{rebuild}}(r)
=T_{\mathrm{bord}}+T_{\mathrm{rev}}+T_{\mathrm{EAM}}
+T_{\mathrm{grid\text{-}rev}}+T_{\mathrm{grid\text{-}fwd}}+T_{\mathrm{FFT}}
\]

对应输出：`T1_rebuild_send_bytes`。

| 为什么 rebuild 用 borders 替换 forward | borders 当步已经完成等价的一次坐标同步；估算器按“重建步主通信是 borders”记账 |
|---|---|
| 为什么没有 exchange | 见 §3；静态构型无法估迁移量 |

无 PPPM 时，三个网格相关项为 0。

### 6.3 PPPM 相关通信（仅 long）

网格尺寸 \((N_x,N_y,N_z)\) 按 LAMMPS `estimate_ik_error` 同类逻辑由 \(\alpha,r_c,q_i,N,L_d\) 推出；order 固定 \(p=5\)。

\[
M=N_x N_y N_z
\]

网格 halo 拷贝点数（均匀网格近似）：

\[
N_{\mathrm{grid\text{-}halo}}
=\sum_{d:\,P_d>1}
2\cdot\frac{p}{2}\cdot A_d^{\mathrm{grid}}
\]

其中 \(A_d^{\mathrm{grid}}\) 是网格空间上“前面维已扩 halo、后面维仍为本地块”的截面积（与 CommBrick 面积公式同构）。

\[
T_{\mathrm{grid\text{-}rev}}
=N_{\mathrm{grid\text{-}halo}}\times 8
\times\mathrm{scale}(r)
\]

\[
T_{\mathrm{grid\text{-}fwd}}
=N_{\mathrm{grid\text{-}halo}}\times 3\times 8
\times\mathrm{scale}(r)
\]

\[
T_{\mathrm{FFT}}
\approx\frac{M}{R}\times 160\times\Bigl(1-\frac{1}{R}\Bigr)
\times\mathrm{scale}(r)
\]

| 符号 | 含义 |
|---|---|
| \(M\) | PPPM 网格总点数 |
| \(p\) | 插值阶（实现固定 5） |
| 160 | 每网格点 remap 通信量的经验/拓扑近似系数（字节） |
| \(1-1/R\) | 去掉“完全本地、无需网络”的份额 |
| \(\mathrm{scale}(r)\) | 按该 rank 原子负载缩放 |

**FFT remap 是 long 模式主要近似项**；short 模式无此项。

---

## 7. 计算量 C1

### 7.1 均匀密度下的邻居与 ghost

\[
N_{\mathrm{list}}(r)
\approx\frac12\,N_{\mathrm{local}}(r)\,\rho\,\frac{4\pi}{3}\,g^3
\]

\[
N_{\mathrm{pair}}(r)
\approx\frac12\,N_{\mathrm{local}}(r)\,\rho\,\frac{4\pi}{3}\,r_c^3
\]

\[
N_{\mathrm{ghost}}(r)
\approx\max\Bigl(0,\;
\rho\prod_{d}(l_d+2g)-\frac{N}{R}
\Bigr)\times\mathrm{scale}(r)
\]

| 符号 | 含义 | 假设 |
|---|---|---|
| \(N_{\mathrm{list}}\) | 邻居表条目数（half list） | 球体积 \(\times\rho\)，因子 \(1/2\) 对应 half neighbor |
| \(N_{\mathrm{pair}}\) | 进入力 cutoff 的对数 | 用 \(r_c\) 而非 \(g\) |
| \(N_{\mathrm{ghost}}\) | ghost 原子数估计 | 子域外扩 \(g\) 的壳层；再按负载缩放 |

**为什么用均匀密度：**  
精确邻居需要 \(O(N)\) 建表；估算器目标是静态闭式/轻量重放，接受密度均匀假设。

### 7.2 各计算项

**积分（Verlet 一类）：**

\[
C_{\mathrm{integrate}}=18\,N_{\mathrm{local}}(r)
\]

**LJ + Coulomb（`lj/cut/coul/*`）：**

\[
C_{\mathrm{pair}}^{\min}=8\,N_{\mathrm{list}}+45\,N_{\mathrm{pair}}
\]

\[
C_{\mathrm{pair}}^{\max}=8\,N_{\mathrm{list}}+50\,N_{\mathrm{pair}}
\]

系数含义（理论 DP-ops 模型，由 PMU 对拍验证，不代表指令条数）：

- 8：邻居候选相关操作量级；
- 45–50：每个力 pair 的 LJ/Coulomb 内核 DP 等价 ops 区间。

**EAM 下界：**

\[
C_{\mathrm{EAM}}^{\min}
=8\,N_{\mathrm{list}}+62\,N_{\mathrm{pair}}
+20\bigl(N_{\mathrm{local}}+N_{\mathrm{ghost}}\bigr)
\]

**EAM 上界：**

\[
C_{\mathrm{EAM}}^{\max}
=8\,N_{\mathrm{list}}+66\,N_{\mathrm{pair}}
+20\bigl(N_{\mathrm{local}}+N_{\mathrm{ghost}}\bigr)
\]

62–66 是用 Cu 256/4r 建模并由独立 Cu 500/4r 场景复核后的 EAM pair
DP-ops 区间；最终仍取上下界中点。

**PPPM（有 kspace 时）：**

\[
C_{\mathrm{PPPM}}
=40\,N_{\mathrm{local}}\,p^3
+20\,\frac{M}{R}\log_2 M
+20\,\frac{M}{R}
\]

实现里后两项的网格份额会再乘 \(\mathrm{scale}(r)\)。粗分：

- \(40 N_{\mathrm{local}} p^3\)：粒子↔网格插值；
- \(20(M/R)\log_2 M\)：FFT；
- \(20(M/R)\)：网格上 Poisson / 其它局部网格工作。

### 7.3 汇总

\[
C_{\min}=C_{\mathrm{integrate}}+C_{\mathrm{pair/EAM}}^{\min}+C_{\mathrm{PPPM}}
\]

\[
C_{\max}=C_{\mathrm{integrate}}+C_{\mathrm{pair/EAM}}^{\max}+C_{\mathrm{PPPM}}
\]

\[
C_1=\frac{C_{\min}+C_{\max}}{2}
\]

| 输出字段 | 值 |
|---|---|
| `C1_steady_ops_min` | \(C_{\min}\) |
| `C1_steady_ops_max` | \(C_{\max}\) |
| `C1_steady_ops_midpoint` / `C1_ops` | \(C_1\) |

---

## 8. 明确不做 / 刻意简化的部分

| 项目 | 处理 | 原因 |
|---|---|---|
| `exchange` 原子迁移 | **不计入 T1** | 需要动力学位移；静态构型下通常 \(N_{\mathrm{migrate}}\approx 0\) |
| MPI 集合通信（thermo 等） | **排除** | assumptions: `collectives excluded` |
| MPI 消息头 / 协议开销 | **不计** | 只对齐应用 payload |
| 非均匀密度邻居 | 用 \(\rho\) 近似 | 避免建真实 neighbor list |
| borders 打包宽度 | 固定 6 或 7 doubles | 不扫描运行时 fix 动态附加字段 |
| FFT remap | \(160\times(1-1/R)\) | 拓扑级近似，非精确通信调度 |
| 多步平均 / rebuild 频率 | 只给单步 steady 与 rebuild 两档 | 摊销需另乘 \(1/I_{\mathrm{rebuild}}\) |

JSON 中的 `assumptions` 字段与上表一致，可读作机器可读的模型声明。

---

## 9. 输出怎么读

### 9.1 全局摘要 `summary.*`

对所有 rank 的对应字段给 `sum / average / minimum / maximum`。

### 9.2 每 rank `ranks[]`

| 字段 | 含义 |
|---|---|
| `nlocal` | \(N_{\mathrm{local}}(r)\) |
| `nghost_estimate` | \(N_{\mathrm{ghost}}(r)\) |
| `neighbor_entries_estimate` | \(N_{\mathrm{list}}(r)\) |
| `force_pairs_estimate` | \(N_{\mathrm{pair}}(r)\) |
| `communication.forward_bytes` 等 | 各通道字节 |
| `T1_steady_send_bytes` | 稳态 T1 |
| `T1_rebuild_send_bytes` | 重建 T1（无 exchange） |
| `computation.*` | 分项 ops |
| `C1_*` | 计算量 |

### 9.3 其它

- `swap_geometry`：密度公式下的 slab 体积明细；
- `pppm`：网格尺寸、gewald、每 rank 网格通信字节（无 kspace 则为 null）。

---

## 10. 和真实 LAMMPS 对拍

启用流水线 PMU 捕捉：

```bash
FLOPS_CAPTURE=1 FLOPS_STEPS=100 FLOPS_REPEATS=3 \
  ./run_noc_pipeline.sh short 16 lialocl 2688 both
```

`profile_lammps_flops.sh` 对每个 rank 做三次 `run(100)-run(0)`，按 SIMD lane
加权 Intel `FP_ARITH_INST_RETIRED`，逐 rank 取中位数。raw event `0xC7` 必须使用：

```text
0x01 scalar double（注意：0x02 是 scalar single）
0x04 128-bit packed double
0x10 256-bit packed double
0x40 512-bit packed double
```

比较器：

```bash
python3 validate_static_cost.py \
  static_cost_estimate.json wse_plan.json flops_profile/flops_summary.json \
  -o static_cost_validation
```

生成 `static_cost_validation.txt/.json`，默认闸门为 T1 2%、C1 全局 10%、
逐 rank 15%。

LiAlOCl 2688 / 16r 当前结果：

| 指标 | 估算 | 真实 | 全局中点误差 | 逐 rank 最大绝对误差 |
|---|---:|---:|---:|---:|
| steady T1 | 1,175,040 B | 1,175,040 B | 0.000% | 0.792% |
| rebuild T1（无 exchange） | 1,958,400 B | 1,958,400 B | 0.000% | 0.473% |
| steady C1 | 11,315,180 DP ops | 11,573,193 DP ops | -2.229% | 5.597% |

C1 理论区间为 `[10,814,361, 11,816,000]` DP ops/step，15/16 个 rank 的
PMU 值落入各自区间，整体 PASS。

Cu EAM 256 / 4r 当前结果：

| 指标 | 估算 | 真实 | 全局中点误差 | 逐 rank 最大绝对误差 |
|---|---:|---:|---:|---:|
| steady T1 | 66,560 B | 66,560 B | 0.000% | 0.000% |
| rebuild T1（无 exchange） | 91,520 B | 91,520 B | 0.000% | 0.000% |
| steady C1 | 462,704 DP ops | 476,715 DP ops | -2.939% | 6.273% |

C1 理论区间为 `[451,692, 473,716]` DP ops/step，整体 PASS。独立的 Cu
500/4r 复核场景得到：估算中点 873,798、PMU 893,594 DP ops/step，全局误差
-2.215%、逐 rank 最大绝对误差 2.871%，不是只对 256 原子单点拟合。

此前曾观察到 C1 约 6.8 倍的表面偏差，根因不是下游或 C1 公式，而是 raw PMU
把 `0x02`（scalar single）误标为 scalar double，漏掉了主要的 scalar-double
计数；修正为 `0x01` 后恢复到上述误差。

误差主要来源：

1. **正式 timestep 积分后坐标 ≠ 静态初始坐标**（short 残差主因）；
2. **Grid3d 离散分块与 FFT remap 拓扑近似**（long 主因）；
3. C1 是理论 DP-ops 区间，可与正确编码且按 SIMD lane 加权的 PMU DP arithmetic
   ops 比较，但不等于 CPU 指令数或 cycles。

---

## 11. 最小例子（帮助建立数量级直觉）

设某 short、`atom_style charge`、无 PPPM 的 rank：

- 坐标重放得 \(N_c=100\)；
- 则  
  \(T_{\mathrm{fwd}}=100\times24=2400\) B，  
  \(T_{\mathrm{rev}}=2400\) B，  
  \(T_{\mathrm{steady}}=4800\) B；  
  \(T_{\mathrm{bord}}=100\times56=5600\) B，  
  \(T_{\mathrm{rebuild}}=5600+2400=8000\) B。
- 若 \(N_{\mathrm{local}}=168\)，\(\rho,g,r_c\) 已知，再代入 §7 得 \(N_{\mathrm{list}},N_{\mathrm{pair}}\) 与 \(C_1\)。

---

## 12. 相关文件

| 文件 | 作用 |
|---|---|
| `estimate_lammps_cost.py` | 实现 |
| `profile_lammps_flops.sh` | 每 rank PMU DP-ops 捕捉 |
| `validate_static_cost.py` | T1/C1 同口径自动比较与闸门 |
| `静态T1_C1估算器说明.md` | 本文 |
| `LAMMPS计算与通信理论模型.md` | 更完整的 LAMMPS 通信/计算理论（含 exchange 定义式等） |

若理论文档与本文冲突，**以本文件 + `estimate_lammps_cost.py` 的实现为准**（例如 rebuild T1 不含 exchange）。
